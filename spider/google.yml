engine: http://127.0.0.1:3000
webpage: https://google.com

# Basic Flow
flow:
  - name: Page 1
    step:

      # Test searching
      - element: .gLFyf.gsfi
        write: crawler
      - element: .gLFyf.gsfi
        action: Enter
      
      # Wait until 2 (second)
      - delay: 2

      # Remove text on search field
      - extract:
          element: .kno-rdesc
          name: sample-1
          result: html

      # Remove text on search field
      - extract:
          element: .hgKElc
          name: sample-2
          result: text

      # Remove text on search field
      - extract:
          element: .Wt5Tfe
          result: text
